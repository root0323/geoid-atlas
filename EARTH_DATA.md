# 지구: KN 전용 3D / 지도: KN·EGM 비교

## 화면 구성

지구 3D 화면은 모델 탭 없이 **KNGeoid18만** 표시합니다. 동아시아 지도는 기존 **KNGeoid18 / EGM2008** 탭과 관측점 표시 비율 100·50·25·10·5%, 관측점 팝업, 통계 및 CSV 기능을 유지합니다. 지도 모델을 EGM으로 바꿔도 지구 3D는 KN입니다. 3D에서 지도를 다시 열면 지도 쪽 모델·표시 비율은 유지됩니다.

## 자료 출처

사용자가 첨부한 `KNGeoid18.dat`입니다. 이미 업로드한 `app-v6.html` 안의 KN 격자와 `sampleKN` 함수를 재사용합니다. 원본 관측자료, KN 격자, EGM2008 파일은 변경하지 않았습니다.

- 위도: 32.99796–39.00208° N
- 경도: 123.99796–131.998° E
- 격자: 368 × 490, 간격 0.01636°
- 지오이드고: 15.053–33.608 m
- 첨부 원본 SHA-256: `9d88e22cb05387c6a07a342fd6b77c4221184d9f636a4b84687319d6dbd351b6`

첨부 파일명과 기존 해석에 따라 사용한 자료입니다. 파일 자체에 부족한 기준계 정보를 임의로 보완하거나 새로운 기관 인증·정확도 주장을 추가하지 않습니다.

## 시각화와 한계

**이 파일은 전 지구 모델이 아닙니다.** KN 자료 범위의 높이만 색상과 굴곡으로 표시합니다. 그 밖은 중력장 값을 만들어 채우지 않고 회색의 기준 타원체만 그립니다. 회색은 `N=0`을 뜻하지 않습니다. 외부 영역을 조회하면 `null`을 반환합니다.

지리 좌표를 표시 지지면 WGS84 타원체(a=6378137 m, 1/f=298.257223563)에 배치하고, 표면 법선 방향으로 `N × 확대 배율`을 더합니다. 기본 확대 배율은 12,000×이며 1×와 확대 배율 조절을 제공합니다. 배율은 원자료와 지도 수치에 영향을 주지 않습니다. KN 영역 확대 버튼은 한반도 주변 3D 부분을 자세히 보는 카메라 조작이며 모델 전환 탭이 아닙니다.

회색 지지면의 지명 없는 윤곽선은 기존 일반화된 경계로 위치 파악만 돕습니다. 지역 색상면의 경계는 파일의 자료 범위이며, 지형이나 실제 절벽이 아닙니다. 경계 측면 벽을 생성하지 않습니다.

화면망은 원격자 노드를 지구 전체 보기에서 8칸, 지역 확대 보기에서 PC 4칸·모바일 6칸마다 추출하며 마지막 경계를 포함합니다. 값의 조회는 기존 4점 쌍선형 보간을 그대로 사용합니다. 생략된 미세 특징이나 화면 소수 자릿수는 모델 정확도를 뜻하지 않습니다.

첨부 파일에 기준계 실현·수직기준·조석계 등의 상세 메타데이터가 부족하여 기준계 변환을 적용하지 않았습니다. 시각적 지지 타원체를 선택한 것이 KN 제품 자체의 기준을 검증했다는 뜻은 아닙니다. 정밀 측량용 기준면이나 공인 정확도 검증 결과가 아닙니다.

## 검증

`python tests/test_earth_kn.py`가 PC와 두 모바일 너비에서 화면 모델, 지역 범위 밖 결측, 경계 및 화면망 수치, 1× 배율, 출처 표시, 원자료 해시, 지도 모델·표시 비율·팝업과 다른 네 천체 기능을 검사합니다. EGM96을 추가하지 않았으며 EGM2008은 기존 지도 비교에만 사용합니다.
