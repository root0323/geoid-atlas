# 지구 전체: EGM2008 3D / 지도: KN·EGM 비교

## 현재 화면

지구 3D는 모델 탭 없이 **EGM2008 전 지구 실제 수치 자료**를 표시합니다. 대륙, 바다, 북극과 남극 모두 같은 전 지구 모델입니다. 한반도 KN 자료를 세계로 외삽하거나 지역 밖을 가상값으로 채우지 않습니다. 이전 KN 전용 3D 파일은 과거 구현으로 남아 있지만 현재 진입 페이지에서 불러오지 않습니다.

동아시아 지도는 **KNGeoid18 / EGM2008** 탭, 관측점 표시 비율 100·50·25·10·5%, 작은 팝업, 통계와 CSV를 그대로 유지합니다. 지도에서 KN을 선택해도 전 지구 3D의 모델은 EGM2008입니다. 화면용 전 지구 격자로 기존 관측점 비교를 재계산하지 않았습니다.

## 원자료와 출처

- 모델: **NGA EGM2008**. [NGA 공식 모델 설명](https://earth-info.nga.mil/index.php?dir=wgs84&action=wgs84)
- 사용한 파일: [PROJ/OSGeo 배포 us_nga_egm08_25.tif](https://cdn.proj.org/us_nga_egm08_25.tif)
- 생성·배포 관계: NGA EGM2008 모델 → GeographicLib 생성 격자 → PROJ GeoTIFF 배포. [설명과 Public Domain 이용 조건](https://cdn.proj.org/us_nga_README.txt)
- 연구: Pavlis, Holmes, Kenyon & Factor (2012), *The development and evaluation of the Earth Gravitational Model 2008 (EGM2008)*. [DOI:10.1029/2011JB008916](https://doi.org/10.1029/2011JB008916)
- 원본 SHA-256: `4191d471eefebf24091b56dbc604353cb3b8cf8cc70e448bb9ae56a272bef17a`

기존 지도 EGM2008과 동일한 원자료 해시입니다. 서로 다른 세대의 EGM96을 대신 넣거나 KN과 혼합하지 않았습니다.

## 처리와 해상도

원본은 2.5분(1/24°) 간격, 4,321 × 8,640개 노드입니다. GeoTIFF 픽셀 중심이 실제 격자 좌표라는 정의를 확인한 뒤 24칸마다 추출했습니다. 웹 파일은 1° 간격의 **181 × 360 = 65,160개** 값입니다. 북위 90°부터 남위 90°까지, 경도 -180°부터 179°까지 저장하고 180° 경계는 순환 연결합니다.

저장은 0.0001 m 단위로 반올림하지만 이 자릿수는 모델 정확도가 아닙니다. 평균값 제거, 임의 평활화, 결측값 채움, 지역 모델의 외삽은 하지 않았습니다. 전 지구 단순화 격자는 원본의 모든 미세 특징을 보존하지 않습니다.

| 자료 | 최솟값 | 최댓값 |
|---|---:|---:|
| 원본 2.5분 격자 | -106.9087219 m | +85.8237839 m |
| 웹 1° 격자 | -106.5160 m | +84.6133 m |

화면의 삼차원 격자 간격은 PC 2°, 모바일 3°이며 웹 격자를 쌍선형 보간합니다. 색은 미터 단위 N을 표현하고, 회전 시 조명 효과는 별도로 적용합니다. 지도 팝업의 EGM2008은 기존 2.5분 격자 보간을 유지하므로 3D의 간략화된 지점 표시와 소수점까지 일치할 필요는 없습니다.

## 기준면·시각적 배율

지오이드고 N은 **WGS84 기준 타원체**에 대한 높이입니다. a=6378137 m, 1/f=298.257223563을 사용하고 측지위도에 해당하는 타원체의 법선 방향으로 N을 적용합니다.

`표시 위치 = 타원체 위치 + (표시 배율 × N) × 바깥쪽 단위 법선`

기본 배율은 10,000×입니다. **실제 비율 1×**는 높이 과장을 없애며 거의 매끈한 타원체처럼 보입니다. 색 범위와 N의 수치는 배율에 따라 바뀌지 않습니다. 참조선은 점선으로만 표시하여 음수 N 표면을 불투명 타원체로 가리지 않습니다. 일반화된 해안·국경 윤곽은 위치 파악을 돕는 선일 뿐 지형 자료가 아닙니다.

이 화면은 산과 계곡 또는 관측 지표 고도를 그린 것이 아닙니다. 원심·중력 퍼텐셜을 새로 구면조화 합성한 결과가 아니라 공개 EGM2008 지오이드고 격자의 시각화입니다. 기준계 실현·조석계의 추가 보정이나 정밀 측량 정확도 검증을 수행하지 않았습니다.

## 보존·재현·검증

`app-v6.html`의 원본 관측자료·KN 격자 및 `data/egm2008.json`의 지역 값은 바이트 단위로 보존합니다. `scripts/build_earth_global.py`는 해시를 확인하고 전 지구를 추출하며, 기존 지역 EGM 검증 지점에서 같은 원자료를 사용했는지 독립 보간으로 확인합니다.

`data/earth-global/summary.json`에 출처, 원본/출력 해시, 검증 지점 및 보존 파일 해시를 저장했습니다. `tests/test_earth_global.py`는 실제 노드 수치, 모든 대륙·해양을 포함하는 격자 범위, 경도 이음새와 양 극점, 1×, 출처 표시, PC·모바일, 자료 로딩 실패 시 가상 대체 금지와 기존 지도·다른 천체 기능을 검사합니다.
